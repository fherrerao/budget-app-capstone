<%= render 'header'%>

<div id="transactions">
  <% @entities.each do |entity| %>
    <%= render entity %>
    
  <% end %>
</div>

<div class="actions my-3 p-3 bg-success d-grid container">
  <%= link_to 'New transaction', new_group_entity_path, class: "btn btn-success"%>        
</div>
